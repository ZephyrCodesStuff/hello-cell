[workspace]
members = [
    "ps3",
    "ps3-macros",
    "examples/*",
]
exclude = ["moldier"]
resolver = "2"

[profile.dev]
codegen-units = 1
panic = "abort"
debug = 1
lto = "fat"

[profile.release]
codegen-units = 1
panic = "abort"
debug = 1
lto = "fat"
